# squid-proxy

Squidによるホワイトリストプロキシ。

# Quick Start

```bash
git clone https://github.com/v1tam1nb2/squid-proxy.git
cd squid-proxy
docker compose up -d
```

# whitelist

github.comのみアクセスを許可する設定をしている。

```
github.com
```
